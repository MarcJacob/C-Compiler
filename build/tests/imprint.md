This folder contains test C Source code files.

Keep clean of any build products. Every .cpp file directly in this directory should be single-translation-unit program roots.
If a test case requires multiple files, place every one of them (*including* the root translation unit / 'main' file) into its own folder.
Ensure the root file / folder is named appropriately.